services:
  #====================================================
  # ARQUITECTURA INTERNA (MI EMPRESA)
  #====================================================
  payment-management-service:
    build: ./payment-management-service
    ports:
      - "9050:9050"
    networks:
      - internal-net
    depends_on:
      - electric-bill-service
    environment:
      - MICROSERVICE_ELECTRIC_BILL_URL=${ELECTRIC_BILL_URL}      

  electric-bill-service:
    build: ./electric-bill-service
    ports:
      - "5000:5000"
    networks:
      - internal-net
    depends_on:
      - toxiproxy
    environment:
      - FEIGN_CONNECT_TIMEOUT=${CONNECT_TIMEOUT_MS}
      - FEIGN_READ_TIMEOUT=${READ_TIMEOUT_MS}
      - EXTERNAL_PROVIDER_URL=${PROVIDER_URL}
      - SPRING_PROFILES_ACTIVE=docker   

  #====================================================
  # MONITOREO
  #====================================================
  prometheus:
    image: prom/prometheus
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - internal-net

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
    networks:
      - internal-net
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/datasource.yml:/etc/grafana/provisioning/datasources/datasource.yml

      
  #====================================================
  # PUENTE HACIA EL EXTERIOR
  #====================================================

  toxiproxy:
    image: ghcr.io/shopify/toxiproxy:latest
    container_name: toxiproxy-api
    ports:
      - "8474:8474" # API para controlar Toxiproxy
      - "6000:6000"
    networks:
      - internal-net
      - external-net
  # volumes:
  #   - ./toxyproxy/toxiproxy-config.json:/etc/toxiproxy/config.json
  # command:
  #   - "-config=/etc/toxiproxy/config.json"
  
  #====================================================
  # ARQUITECTURA EXTERNA (OTRA EMPRESA)
  #====================================================

  wiremock:
    image: wiremock/wiremock:latest
    container_name: wiremock-external-api
    volumes:
      - ./external-api/mappings:/home/wiremock/mappings
      - ./external-api/__files:/home/wiremock/__files  
    # ports:
    #   - "8080:8080"
    expose:
      - "8080"
    networks:
      - external-net

#====================================================
# DEFINICIÓN DE REDES
#====================================================
networks:
  internal-net:
    driver: bridge
  external-net:
    driver: bridge

volumes:
  grafana-data: